# Data layout

Place local experiment data like this:

- `data/Humming Audio/<Song Name>/*.wav` for humming clips
- `data/Original Songs/` for original tracks
- `data/demucs_output/` for separated stems

Generated artifacts go under `data/processed/datasets/` and should not be committed.
